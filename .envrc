#!/bin/bash
# direnv configuration for dbt project
# This file is automatically loaded when you cd into this directory

# Load poetry virtual environment
layout poetry

# Set dbt environment variables
export DBT_KEYFILE_PATH=keys/keyfile.json
export DBT_TARGET=prod

# Optional: Uncomment to override project settings
# export DBT_PROJECT_ID=your-project-id

echo "✓ dbt environment loaded (target: $DBT_TARGET)"
